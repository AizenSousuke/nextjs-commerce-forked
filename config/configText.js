// Use this config json to populate data across the app
const configText = {
    "store_title": "The Everything Store",
    "cart_message": "Add something to your cart first."
}

export default configText;